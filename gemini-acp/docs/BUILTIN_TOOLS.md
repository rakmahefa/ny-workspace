# Builtin filesystem discovery

## `glob`
Finds filesystem paths matching a bounded glob pattern inside the configured sandbox.

## `list_directory`
Lists the direct children of a directory in deterministic sorted order with bounded output.
